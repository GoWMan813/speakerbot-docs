{
  "name": "@speakerbot/docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "generate": "nuxi generate",
    "preview": "nuxi preview",
    "deploy": "yarn generate && wrangler pages publish .output/public"
  },
  "devDependencies": {
    "nuxt": "^3.1.0",
    "@nuxt-themes/docus": "^1.6.0"
  }
}
